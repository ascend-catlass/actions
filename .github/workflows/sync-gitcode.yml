name: Sync GitCode master and tags

on:
  schedule:
    # 02:00 Asia/Shanghai (18:00 UTC)
    - cron: "0 18 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sync-gitcode-master-and-tags
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out the GitHub mirror
        uses: actions/checkout@v4
        with:
          repository: ascend-catlass/catlass
          ref: master
          fetch-depth: 1
          fetch-tags: false
          ssh-key: ${{ secrets.CATLASS_DEPLOY_KEY }}
          path: catlass-mirror

      - name: Fetch GitCode master and tags
        shell: bash
        working-directory: catlass-mirror
        run: |
          set -euo pipefail

          git remote add gitcode https://gitcode.com/cann/catlass.git
          git fetch --force --prune --prune-tags gitcode \
            +refs/heads/master:refs/remotes/gitcode/master \
            +refs/tags/*:refs/tags/*

      - name: Update GitHub master and tags
        shell: bash
        working-directory: catlass-mirror
        run: |
          set -euo pipefail

          git push --force origin \
            refs/remotes/gitcode/master:refs/heads/master
          git push --force --prune origin \
            'refs/tags/*:refs/tags/*'
