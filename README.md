# ascend-catlass/actions

Shared GitHub Actions automation for the `ascend-catlass` organization.

The workflows in `.github/workflows/` are reusable workflows called by thin
event-triggering workflows in their consumer repositories. Composite actions
and their scripts live under `.github/actions/`.
